import { StyleSheet } from 'react-native';

export const generalStyles = StyleSheet.create({
    fullSizeAbsolute: {
        position: 'absolute',
        top: 0,
        bottom: 0,
        left: 0,
        right: 0,
    }
});
